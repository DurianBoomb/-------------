问一下这个页面的入场动画问题。

我在工具集首页实现了卡片弹性入场动画，但在微信小程序中没有触发。我的实现如下：

**模板**：
```html
<view class="tool-card" :style="{ animationDelay: (idx * 0.1) + 's' }">
```

**CSS**：
```css
.tool-card {
  animation-name: cardEntrance;
  animation-duration: 0.5s;
  animation-timing-function: cubic-bezier(0.34, 1.56, 0.64, 1);
  animation-fill-mode: both;
}

@keyframes cardEntrance {
  0% { opacity: 0; transform: translateY(60rpx); }
  100% { opacity: 1; transform: translateY(0); }
}
```

**问题**：动画完全没有触发，卡片打开页面就直接显示了（没有从上方掉落的入场效果）。

**请问**：
1. `rpx` 单位在 `@keyframes` 中是否有效？还是必须用 `px`？
2. `animation-fill-mode: both` + 内联 `animationDelay` 在小程序中是否有已知的兼容问题？
3. 这个动画在你的 React 代码中是如何实现的？能否贴出你的 CSS 代码片段供参考？
