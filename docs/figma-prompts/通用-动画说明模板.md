请详细讲解当前页面的所有动画效果，方便我移植到 Vue 2 + uni-app。

请按这个格式说明：

## 1. 入场动画
- 哪些元素有入场动画？
- 每个元素用了什么 @keyframes？
- animation-duration、animation-timing-function、animation-delay 分别是多少？
- animation-fill-mode 是什么？

## 2. 交互反馈
- 点击/按压时有哪些视觉反馈？
- 用了 CSS 的什么实现方式？（:active / hover-class / @keyframes / transition）
- 变化前后的具体样式值（如 scale 从多少到多少，用时多少）

## 3. 其他动效
- 页面中是否有持续循环的动画（如呼吸、浮动）？
- 如果有，周期多长、幅度多大、缓动曲线是什么？

## 4. Vue / uni-app 移植建议
- 上述动画在微信小程序中是否都能正常工作？
- 有哪些需要注意的兼容性问题？
- 建议用 transition 还是 animation 来实现？

请逐一列出，不要省略细节。
