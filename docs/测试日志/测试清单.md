# 测试清单

看看哪些功能能跑通、哪些还卡着。


## 一、基础

1. 启动 → 首页正常显示（标题、狐狸动画、卡片入场上滑）—— 1
2. 未登录 → 头像区显示"未登录" —— 1
3. 点头像 → 跳到 ucenter 页面 —— 1
4. 微信登录 —— 1（uni-id 自带流程）
5. 登录后 → 头像正确显示 —— 1（avatar_file 路径已修）
6. 重新打开 → 登录态保持 —— 1


## 二、趣味测试首页

7. 首页 → 点"趣味测试" → 跳到小工具首页 —— 1
8. 标签池显示标签（数据来自服务端 getTagList）—— ☐ 需要打标验证
9. "换一批"点一下卡片重新入场 —— 1
10. "随便测测"点一下 → 随机选标签跳答题 —— ☐ 可以随机选择标签，但对应题目还没准备好
11. 精选推荐显示前 3 个热门标签 —— ☐ 是否是热门不清楚，但有三个
12. 精选卡片右侧 ☆ 点一下收藏 —— ☐（依赖 token 校验）需要我登录


## 三、搜索结果页

13. 点"搜点啥" → 跳到搜索页 —— 1
14. 手风琴分类展开/收起 —— ☐ 收起效果需要抄figma
15. 点分类 → 显示该分类标签 —— 1
16. 点标签 → 跳到答题页 —— 1


## 四、答题

17. 加载问卷 → 显示题目和选项 —— 1（问卷数据从 Coze 生成后上传）
18. 选项文本每道题不一样 —— 1（三级池随机组合，还可以增加更多）
19. 选完自动切下一题 —— 1（切的动画效果还需要参考figma调优
20. 答完自动跳结果页 —— 1
21. 加载不到问卷 → toast 提示并返回 —— ✅ 已有


## 五、结果页

22. 显示匹配结果（emoji + 名称 + 描述） —— 1
23. 雷达图渲染 —— 1（之前硬编码 5 维，需适配动态维度，但好像已经适配完了）
24. 点赞 👍 / 点踩 👎 —— ☐（依赖 token 校验）
25. "再来一次" → 重新答题 —— 0 直接返回工具首页了
26. 答题记录保存到 survey-answers 表 —— ☐（依赖 token 校验）


## 六、个人相关（依赖登录）

27. 答题历史 📋 —— ☐ **卡 token 校验**
28. 照照镜子 🥸（已测标签列表） —— ☐ **卡 token 校验**
29. 收藏 ⭐ —— ☐ **卡 token 校验**
30. 点赞/点踩 —— ☐ **卡 token 校验**
