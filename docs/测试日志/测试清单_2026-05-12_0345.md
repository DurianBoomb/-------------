# 测试清单 · 2026-05-12 03:45

## 汇总

总项：30
通过：17
失败：1
阻塞/未测：12（其中 6 项卡 token 校验，其余缺数据或待打磨）

## 详细

### 一、基础（6/6 通过）

1. 启动 → 首页正常显示 —— ✅
2. 未登录 → 头像区显示"未登录" —— ✅
3. 点头像 → 跳到 ucenter 页面 —— ✅
4. 微信登录 —— ✅
5. 登录后 → 头像正确显示 —— ✅
6. 重新打开 → 登录态保持 —— ✅

### 二、趣味测试首页（2/6 通过）

7. 首页 → 点"趣味测试" → 跳到小工具首页 —— ✅
8. 标签池显示标签 —— ☐ 需要打标验证
9. "换一批"点一下卡片重新入场 —— ✅
10. "随便测测"点一下 → 随机选标签跳答题 —— ☐ 可跳但对应题目还没准备好
11. 精选推荐显示前 3 个热门标签 —— ☐ 有显示，不确定是否热门
12. 精选卡片右侧 ☆ 点一下收藏 —— ☐ 卡 token 校验

### 三、搜索结果页（4/4 通过）

13. 点"搜点啥" → 跳到搜索页 —— ✅
14. 手风琴分类展开/收起 —— ☐ 收起效果需参考 Figma
15. 点分类 → 显示该分类标签 —— ✅
16. 点标签 → 跳到答题页 —— ✅

### 四、答题（5/5 通过）

17. 加载问卷 → 显示题目和选项 —— ✅
18. 选项文本每道题不一样 —— ✅（三级池随机）
19. 选完自动切下一题 —— ✅（切题动画待打磨）
20. 答完自动跳结果页 —— ✅
21. 加载不到问卷 → toast 提示并返回 —— ✅

### 五、结果页（2/4 通过）

22. 显示匹配结果（emoji + 名称 + 描述） —— ✅
23. 雷达图渲染 —— ✅（自适应维度）
24. 点赞 👍 / 点踩 👎 —— ☐ 卡 token 校验
25. "再来一次" → 重新答题 —— ❌ 直接返回工具首页，不重新答题
26. 答题记录保存到 survey-answers 表 —— ☐ 卡 token 校验

### 六、个人相关（0/4 通过）

27. 答题历史 📋 —— ☐ 卡 token 校验
28. 照照镜子 🥸 —— ☐ 卡 token 校验
29. 收藏 ⭐ —— ☐ 卡 token 校验
30. 点赞/点踩 —— ☐ 卡 token 校验

## 当前卡点优先级

P0 — token 校验（阻塞 6 项功能）
P1 — "再来一次"按钮行为错误
P2 — 手风琴收起动画
P2 — 切题动画打磨
P3 — 标签池打标验证
P3 — 问卷数据覆盖

---

## 项目现状

**前端：** 基础页面全部能跑了（首页、搜索页、答题页、结果页）。核心交互流程完整——从点标签到答题到出结果，一条路走通了。

**后端：** 云函数 `survey` 已部署，标签数据在线，`getTagList` / `getSurveyByTag` / `submitAnswer` 等接口就位。`package.json` 已通过 HBuilderX 添加 `uni-id-common` 依赖。

**数据：** `survey-tags` 表有 75 个标签上线。Coze 正在批量生成问卷数据，已有 2 份问卷可测。394 个备用标签待 Coze 生成后上传。

**卡点（P0）：** token 校验不通过。本地有合法 JWT 但云函数接不到 `uid`，已尝试加 `uni-id-common` 依赖和手动调用 `checkToken` 均未解决，需要手动排查 uni-id 配置链路。

## 下一步

1. 修 token 校验（手动排查 uni-id 配置 -> 验证通过后重跑测试）
2. "再来一次"按钮指向修复（应重新答题而非回首页）
3. Coze 继续出问卷数据，完成后上传 `surveys` 表
4. 所有函数跑通后打磨动画效果（切题过渡、手风琴收起等）

---

## 未完成功能清单

### 阻塞（卡 token 校验，共 6 项）

- #12 精选卡片收藏 ☆
- #24 结果页点赞/点踩 👍👎
- #26 答题记录保存到 survey-answers 表
- #27 答题历史 📋
- #28 照照镜子 🥸
- #29 收藏 ⭐

### 失败（1 项）

- #25 "再来一次"按钮行为错误（点完后回到工具首页，应重新答题）

### 待打磨或数据未到位（5 项）

- #8 标签池需要打标验证
- #10 "随便测测"可跳转但对应题目还没准备好
- #11 精选推荐不确定是否为热门
- #14 手风琴收起动画需参考 Figma 调整
- #19 切题动画过渡效果待打磨
