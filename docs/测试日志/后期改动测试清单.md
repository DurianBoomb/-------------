# 后期改动测试清单

> 对应《后期工作计划.md》5 条改动。先改后测，所有项都跑通才算完工。

---

## 通用准备

在开始测试前，确保：
1. 微信开发者工具中登录了测试账号
2. 已删除 `unpackage` 缓存文件夹后重新运行（防热更新 bug）
3. 云函数已上传并部署（`survey` + `pin-system`）

---

## 前置：`common/ad-utils.js` 公用广告函数

| # | 测试项 | 预期 | 结果 |
|---|--------|------|------|
| 1 | `playAd(surveyId)` 在微信环境创建 `RewardedVideoAd` 实例 | 不抛异常，adUnitId 正确 | ☐ |
| 2 | 广告正常播放完毕 → `handleAdReward` 被调用 | 云对象正确接收 surveyId 和 adDuration | ☐ |
| 3 | 广告提前关闭（`res.isEnded = false`）→ 不给奖励，toast 提示 | toast "看完广告才能获得推广" | ☐ |
| 4 | 非微信环境（HBuilderX 模拟器）→ 走 setTimeout 模拟 | 1.5 秒后弹出 toast"模拟广告播放完成" | ☐ |
| 5 | 网络异常时 catch 到错误 → toast"网络异常，请重试" | 不崩溃 | ☐ |

---

## 一、用户画像页改造（`profile.vue`）

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 1 | 数据源切换 | 打开照照镜子页 | 显示所有历史答题记录（不再是去重后的标签列表） | ☐ |
| 2 | 记录总数 | 查看顶部"已确诊 X 个标签" | X = 历史答题总条数（不是标签去重数） | ☐ |
| 3 | 时间显示 | 任意一条卡片 | 卡片下方显示 `YYYY-MM-DD HH:mm` 格式时间 | ☐ |
| 4 | 删除按钮 | 右侧红色"删除"文字按钮 | 点击弹出确认弹窗 | ☐ |
| 5 | 删除确认 | 确认弹窗点"确定" | 该条记录消失，总条数 -1，toast"已删除" | ☐ |
| 6 | 删除取消 | 确认弹窗点"取消" | 无任何变化 | ☐ |
| 7 | 绑定卡快速删除测试 | 相邻卡片重复点击删除 | 各卡片独立删除，互不影响 | ☐ |
| 8 | 空状态 | 清空所有记录后刷新 | 显示"还没有答题记录"引导文案 | ☐ |
| 9 | 卡片点击 | 点任意卡片 | 跳转 `answer-quiz` 答题页，传入对应 tag 和 surveyId | ☐ |

---

## 二、首页改造（`quiz-home.vue`）

### 2.1 档案入口替换

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 1 | 图标替换 | 查看首页头部 2×2 grid | 左下角为 📂 图标 | ☐ |
| 2 | 跳转历史档案 | 点 📂 | 跳转到 `career-history.vue` 历史档案页 | ☐ |

### 2.2 副标题删除

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 3 | 副标题消失 | 查看品牌区 | "正经人谁做测试啊..." 已消失 | ☐ |
| 4 | 布局不变 | 品牌区下方直接接 scroll-view | 没有空白间隙或样式错乱 | ☐ |

### 2.3 看广告推广入口删除

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 5 | 组件移除 | 查看首页 | pin-terminal-entry 金色按钮已消失 | ☐ |

### 2.4 分享文案

| # | 测试项 | 预期 | 结果 |
|---|--------|------|------|
| 6 | 转发分享 | 分享卡片标题显示"快乐大狐狸"（不含"正经人谁做测试啊"） | ☐ |

---

## 三、结果页推广按钮（`result.vue`）

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 1 | 按钮显示 | 完成答题看到结果页 | footer 区出现"📢 推广本问卷"金色按钮，三等分排列 | ☐ |
| 2 | 按钮样式 | 对比"再来一次"和"分享给朋友" | 三个按钮宽度相等，金色/白色/蓝色互补不违和 | ☐ |
| 3 | 广告播放 | 点"推广本问卷" | 调用 `playAd(surveyId)`，弹出广告 | ☐ |
| 4 | 广告完成 → 直接置顶 | 广告完整播放，池未满或 Lv.0 | toast"置顶成功"，pin-pool 新增记录 | ☐ |
| 5 | 广告完成 → 进入候场 | 广告完整播放，池满非 Lv.0 | toast"已进入候场区"，跳转历史档案页 | ☐ |
| 6 | 广告关闭不给奖励 | 提前关闭广告 | toast"看完广告才能获得推广"，无后续操作 | ☐ |
| 7 | 无 surveyId 兜底 | 极端情况 o.surveyId 不存在 | toast"问卷ID不可用"，不崩溃 | ☐ |

---

## 四、「我的生成」操作区改造（`my-surveys.vue`）

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 1 | 三按钮显示 | 查看任意问卷卡片右侧 | 「推广」「预览」「删除」三个文字按钮横向一行排列 | ☐ |
| 2 | 推广按钮颜色 | 查看「推广」 | 暖黄底（`#FFFBEB`）+ 深棕字（`#B45309`） | ☐ |
| 3 | 预览按钮颜色 | 查看「预览」 | 淡蓝底（`#EFF6FF`）+ 蓝字（`#1D4ED8`） | ☐ |
| 4 | 删除按钮颜色 | 查看「删除」 | 淡红底（`#FEF2F2`）+ 深红字（`#991B1B`） | ☐ |
| 5 | 推广功能 | 点「推广」 | 调用 `playAd(item.id)`，广告播放 | ☐ |
| 6 | 推广完成 → 置顶成功 | 广告完整播放 | toast"置顶成功" 或 进入候场跳转 | ☐ |
| 7 | 预览功能 | 点「预览」 | 跳转 `survey-preview` 正常展示问卷 | ☐ |
| 8 | 删除功能 | 点「删除」 | 弹出确认弹窗，确认后删除 | ☐ |

---

## 五、回归检查（已有功能不可改坏）

| # | 测试项 | 操作 | 预期 | 结果 |
|---|--------|------|------|------|
| 1 | 首页其他入口 | 点 🥸 用户画像 | 跳转 profile.vue，数据正常 | ☐ |
| 2 | 首页其他入口 | 点 ⭐ 收藏 | 跳转收藏页，功能正常 | ☐ |
| 3 | 首页其他入口 | 点 🪄 我的生成 | 跳转 my-surveys，卡片加载正常 | ☐ |
| 4 | 结果页原有功能 | "再来一次"按钮 | 正确跳转重试流程 | ☐ |
| 5 | 结果页原有功能 | "分享给朋友"按钮 | 正常触发分享 | ☐ |
| 6 | 结果页底部免责文字 | "测着玩的，别当真 😅" | 仍然展示，未被推广按钮挤压消失 | ☐ |
| 7 | 历史档案页 | 从首页 📂 进入 career-history | 页面正常渲染，三槽位管理台正常 | ☐ |
| 8 | 云函数 survey | 答题、结果、收藏等正常 | 不因改动受影响 | ☐ |
