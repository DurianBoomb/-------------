# 方案 C 工作进度

> 最后更新：2026-05-11 04:13
> 状态：**已完成（实际产出清理后）**

---

## 一、方案 C 的实际产出

方案 C 绕了一圈后清理掉了多余部分，实际留在项目里的东西：

### 保留的
- `问卷生成——Coze工作流提示词（合并版）.md` — 从重写版继承并优化，结果类型规则改为"每个维度一个结果"，增加了约束条目
- `问卷数据结构规范.md` — 验证规则更新（resultTypes 数量必须等于 dims）
- `方案C工作进度` — 本存档文档

### 已清理的
- `entity-data/` 文件夹（12 个实体 JSON） → 删除
- `scripts/validate-survey.js` → 删除
- `scripts/seed-database.js` → 删除
- `实体特征提取——Coze工作流提示词.md` → 删除
- `问卷生成——Coze工作流提示词（轻量版）.md` → 删除

### 已有的（未参与也不受影响）
- `survey` 云函数 — 5/10 先生自行创建，内容完整
- `survey-categories` / `survey-tags` / `surveys` schema — 先生之前建的
- `init_data/` — 今天创建的两份初始数据 JSON，保留了

---

## 二、回到原点后的实际差异

回到"一个 Coze 工作流出题"这个模式后，方案 C 带来的实际变化：

1. **`问卷生成——Coze工作流提示词（合并版）.md`** 比原来 `重写版.md` 多了"resultTypes数量必须等于dims"的约束，输出示例也改为 3 dims → 3 resultTypes
2. **`问卷数据结构规范.md`** 的验证规则同步更新了这条
3. 去掉了一个"把简单事情做复杂"的教训

Coze 工作流、云函数、数据库架构、前端——这些都没变。

---

## 三、教训可记

方案 C 走偏的地方在于：
- 过度设计了"特征提取工作流"，实际场景不需要
- AI 手写了 12 个实体 JSON 给先生增加了审核负担
- 写了不需要的校验脚本和种子脚本
- 核心价值只有：提示词优化 + 数据规范同步更新

**下次遇到类似问题：先确认先生的实际使用场景和规模，不要假设需要自动化/规模化工具。**
