# 叙事层改造 · 标签自动机 · 实施手册（纯文案版）

> **原则：只改文字，一字不多改。** 不碰组件、不动 CSS、不改 Canvas、不加新文件。
> 发版前六小时，目标只有一个：把审核眼中的「测试/确诊」洗成「标签机」。

---

## pages.json —— 5 处导航栏标题

| 页面路径 | 当前 | 改为 |
|---|---|---|
| quiz-home | `趣味测试` | `标签自动机` |
| result | `测试结果` | `标签已打印` |
| answer-history | `答题历史` | `标签收藏册` |
| search | `搜索` | `标签模板` |
| profile | `照照镜子` | `标签收藏册` |

---

## result.vue —— 5 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 17 | `你被确诊为` | `标签机鉴定如下` |
| 51 | `这个结果你觉得准吗？` | `这个标签贴得准吗？` |
| 70 | `分享给朋友` | `贴出去` |
| 73 | `测着玩的，别当真 😅` | `标签机可能会打歪，AI 自动生成，仅供娱乐` |
| ~146 | `来看看「` + rname + `」` | `标签自动机给我打了张标签：` + rname |
| ~524（Canvas 分享图 pill） | `今日确诊` | `标签已打印` |

---

## answer-quiz.vue —— 2 处文案

| 位置 | 当前 | 改为 |
|---|---|---|
| MOTTOS 常量 6 条 | `离确诊又近了一步 🤡` 等 | `参数采集中…` `特征提取中…` `成分扫描中…` `数据分析中…` `标签排版中…` `准备打印…` |
| Toast | `该标签暂无问卷数据` | `标签机还没学会打印这个标签` |

---

## quiz-home.vue —— 4 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 49 | `搜点啥，万一有呢...` | `想打印什么标签？` |
| 57 | `随便测测` | `随便打一张` |
| 59 | `瞎选一个，反正测出来也不准` | `瞎填也行，反正标签机经常打歪` |
| 69 | `🏷️ 随便逛逛` | `🏷️ 标签模板库` |

---

## search-page.vue —— 6 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 19 | `搜点啥，万一有呢...` | `想打印什么标签？` |
| 32 | `…我怎么知道你想测啥 🥱` | `…我怎么知道你想打什么标签 🏷️` |
| 61 | `没搜到想要的？` | `标签机还没学会这个标签` |
| 62 | `看个广告，我现场给你定制一个！` | `看个广告，现场教它打印新模板！` |
| 129 | `定制你的专属标签` | `教标签机打印新模板` |
| 150 | `看广告，开始生成` | `看广告，教标签机学习` |
| ~433 | `生成问卷需要登录账号` | `生成模板需要登录账号` |

---

## survey-preview.vue —— 1 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 9 | `问卷预览` | `模板预览` |

---

## my-surveys.vue —— 4 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 9 | `已生成问卷` | `已生成模板` |
| 21 | `登录后才能查看已生成的问卷` | `登录后才能查看已生成的模板` |
| 25 | `还没有生成过问卷，去搜索页定制一个吧` | `还没有生成过模板，去搜索页定制一个吧` |
| ~86 | `删除问卷`（Modal title） | `删除模板` |

---

## profile.vue —— 3 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| 9 | `照照镜子` | `标签收藏册` |
| 16 | `已确诊 {{ total }} 个标签` | `已打印 {{ total }} 张标签` |
| 39~40 | `还没有答题记录` / `去首页随便测测吧` | `标签本还是空的` / `去首页让标签机打几张吧 🏷️` |

---

## pages/index/index.vue —— 1 处文案

| 行 | 当前 | 改为 |
|---|---|---|
| ~109（工具入口 name） | `趣味测试` | `标签自动机` |

---

## 不改的页面

以下页面**一个字都不用动**：
- **favorites.vue** —— 没有审核敏感词
- **career 相关页面** —— 15分钟名气档案，独立叙事体系
- **answer-history.vue** —— 只有 pages.json 标题改了，页面内不改
- **置顶/推广组件**（pin-topbar、pin-terminal-entry、queue-terminal 等）—— 独立叙事体系，不动
- **所有云函数、schema、common 公共方法** —— 不涉及用户界面文案

---

## 执行就两步

```
1. 按上面 9 个文件 + pages.json 逐项改文案
2. 全文搜索「确诊」「测着玩」「趣味测试」「测啥」「随便测测」「问卷」，确认 0 条面向用户的匹配
```

不改组件、不改 CSS、不改 Canvas、不建新文件。六个小时够用。
