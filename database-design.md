# 问卷系统数据库设计备忘

## 更新时间
2026-04-30 18:34

---

## 已确定设计

### 1. 问卷主表 (surveys) - 设计中
**存储方式**: MongoDB 文档型，题目/维度/结果类型内嵌在问卷文档中

**核心字段**:
- `_id`: 文档主键
- `title`: 问卷标题（最大100字符）
- `desc`: 问卷描述（最大2000字符）
- `status`: 状态（0=草稿，1=已发布，2=已下架）
- `creatorId`: 创建者用户ID
- `dimensions`: 评估维度数组（最多10个）
  - `index`: 维度序号
  - `name`: 维度名称（最大50字符）
- `resultTypes`: 结果类型数组（最多26个）
  - `index`: 类型序号
  - `name`: 类型名称（最大100字符）
  - `desc`: 类型描述（最大5000字符）
  - `imageUrl`: 结果配图URL
  - `ranges`: 维度判定范围（**已确定使用方案A，增加 dimensionIndex 字段**）
    - `dimensionIndex`: 关联的维度序号
    - `min`: 最小值（0-100）
    - `max`: 最大值（0-100）
- `questions`: 题目数组（最多50个）
  - `index`: 题目序号
  - `title`: 题目文本（最大500字符）
  - `dimensionIndex`: 关联维度序号
  - `required`: 是否必答
  - `type`: 题型（single/multiple/fill）
  - `options`: 选项数组
    - `label`: 选项标识
    - `text`: 选项文本（最大200字符）
    - `score`: 分值贡献（0-100）
- `stats`: 统计元数据
  - `submissionCount`: 累计提交次数
  - `avgDuration`: 平均作答时长（秒）

**索引**:
1. `creatorId: 1, createdAt: -1` - 创建者的问卷列表
2. `status: 1, createdAt: -1` - 已发布问卷列表

---

## 已完成工作

### ✅ 问卷主表 (surveys) - 已完成
- [x] **数据库 Schema 设计**
  - [x] 创建 `surveys.schema.json` 文件
  - [x] 定义完整的字段结构（title, desc, dimensions, resultTypes, questions, stats 等）
  - [x] 设置权限规则（创建者可读写）
  - [x] 创建索引（creatorId+createdAt, status+createdAt）
- [x] **云对象开发**
  - [x] 在 `tools` 云对象中添加问卷相关方法
  - [x] `saveSurvey`: 保存问卷（创建/更新）
  - [x] `getSurveyList`: 获取问卷列表
  - [x] `getSurveyDetail`: 获取问卷详情
  - [x] `deleteSurvey`: 删除问卷
  - [x] `updateSurveyStatus`: 更新问卷状态
- [x] **前端集成**
  - [x] 修改 `editor-overview.vue` 的 `doSave` 方法，调用云对象保存数据

## 待完成工作

### P0 优先级（核心功能）
- [ ] **工作台页面（workbench）连接真实数据**
  - [ ] 从云数据库加载问卷列表
  - [ ] 显示真实问卷数据（标题、描述、状态等）
  - [ ] 实现删除、分享等功能
- [ ] **编辑器设置页（editor-setting）数据绑定**
  - [ ] 保存问卷标题和描述
  - [ ] 调用 `saveSurvey` 方法
- [ ] **题目编辑器页面**
  - [ ] 创建题目编辑器页面
  - [ ] 支持单选、多选、填空题型
  - [ ] 题目数据保存到云数据库
- [ ] **维度配置页面**
  - [ ] 创建维度配置页面
  - [ ] 支持添加、编辑、删除维度
  - [ ] 维度数据保存到云数据库
- [ ] **结果类型配置页面**
  - [ ] 创建结果类型配置页面
  - [ ] 支持设置维度判定范围
  - [ ] 结果类型数据保存到云数据库

### P1 优先级（重要功能）
- [ ] **提交记录表 (survey_submissions) 设计**
  - [ ] 字段定义
  - [ ] 索引设计
  - [ ] 数据归档策略（可选）
- [ ] **分享配置表 (survey_shares) 设计**
  - [ ] 字段定义
  - [ ] 访问权限设计（public/password/whitelist）
- [ ] **答题页面开发**
  - [ ] 问卷展示
  - [ ] 答题逻辑
  - [ ] 结果计算
  - [ ] 结果展示

### P2 优先级（辅助功能）
- [ ] **问卷模板表 (survey_templates) 设计**
  - [ ] 字段定义
  - [ ] 官方模板初始化
- [ ] **模板市场页面开发**
  - [ ] 模板列表
  - [ ] 模板详情
  - [ ] 从模板创建问卷
- [ ] **数据统计页面开发**
  - [ ] 提交记录列表
  - [ ] 数据统计图表

### P3 优先级（优化功能）
- [ ] **高级逻辑跳转功能**
  - [ ] skipLogic 字段设计细化
  - [ ] 跳转规则编辑器
- [ ] **批量操作功能**
  - [ ] 批量删除问卷
  - [ ] 批量修改状态
- [ ] **数据导出功能**
  - [ ] 导出提交记录为 Excel
  - [ ] 导出统计报告

---

## 设计决策记录

### 已确定的决策
1. **题目存储方式**: 内嵌在 surveys 文档中，不支持题目复用
2. **维度与结果类型绑定**: 使用方案A，在 ranges 中增加 dimensionIndex 字段
3. **当前阶段**: 仅设计问卷主表，其他表后续设计

### 待决策的问题
暂无

---

## 备注
- 本文档随设计进度更新
- 完成一项工作后，请通知我更新本文档状态
- 如有新的设计决策，请告知我记录到"设计决策记录"章节
